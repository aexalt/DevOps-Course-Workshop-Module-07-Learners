pipeline {
    agent any

    stages {
        stage('dotnet') {
            steps {
                sh dotnet build
            }
        }
        stage('typescript') {
            steps {

            }
        }
    }
}
